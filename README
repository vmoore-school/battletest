# battletest
## pygame recreation of Undertale's systems/mechanics

## Installation
Typically I use uv for python dependency management, so to install battletest run
```
git clone https://github.com/vmoore-school/battletest
uv venv
source .venv/bin/activate
uv pip install -r requirements.txt
```

Then you can simply run the project with `python main.py`
